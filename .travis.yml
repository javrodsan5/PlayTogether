dist: trusty
language: java
jdk: oraclejdk8
services:
  - mysql
cache:
  directories:
  - $HOME/.m2
before_cache:
  - rm -rf $HOME/.m2/repository/net/playtogether/jpa
before_install:
  - chmod +x mvnw
  - mysql -e 'CREATE DATABASE playtogetherdb;'
  - sudo apt-get install jq
  - wget -O ~/codacy-coverage-reporter-assembly-latest.jar $(curl https://api.github.com/repos/codacy/codacy-coverage-reporter/releases/latest | jq -r .assets[0].browser_download_url)
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script: mvn test -Dspring-boot.run.profiles=mysql-travis
after_success:
  - bash <(curl -Ls https://coverage.codacy.com/get.sh) report -r report.xml